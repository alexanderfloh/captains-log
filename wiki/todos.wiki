=ToDos=

  # *`[ done `]* Automatic reloading when files change. 
  # *`[ done `]* Display "Throwable" contents of log file.
  # a way to Filter loggers
  # a way to display only messages >= a certain level
  # *`[ done `]* Search feature should jump to the first found entry
  # Highlight search string in datagrid